const palette = {
    main: {
        text: `var(--txt-main-color)`,
        border: `var(--border-main-color)`,
        bg: `var(--bg-main-color)`,
    },
    sub: {
        text: `var(--txt-sub-color)`,
        border: `var(--border-sub-color)`,
        bg: `var(--bg-sub-color)`,
    }
}

export default palette